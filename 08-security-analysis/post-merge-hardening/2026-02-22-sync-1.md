> [Back to Post-Merge Hardening Index](../post-merge-hardening.md) | [Legitimate Gaps](../post-merge-hardening.md#legitimate-gaps-status)

# Feb 22 Sync 1 — 51 Commits (18 Security)

**Merge commit:** `52c9d7f32` | **Range:** `4dbd4e02f..22940b7b9`
**Date:** 2026-02-22

## Security-Relevant Commits

| SHA | Message | Files | Description |
|-----|---------|-------|-------------|
| `9632b9bcf` | fix(security): fail closed parsed chat allowlist | 1 prod + 3 test + 1 doc | Changes `isAllowedParsedChatSender` (`src/plugin-sdk/allow-from.ts:18`) to return `false` when `allowFrom` is empty (was `return true`). Fixes critical fail-open: an unconfigured allowlist now blocks all senders rather than allowing all. Affects iMessage, BlueBubbles, and all other channel surfaces. Relates to GHSA-5mx2-2mgw-x8rm (BlueBubbles allowlist hardening context). |
| `2ba6de7ea` | refactor(security): make empty allowlist behavior explicit | 1 prod + 2 test | Adds `emptyAllowFrom?: 'deny' \| 'allow'` parameter to `isAllowedParsedChatSender` (`src/plugin-sdk/allow-from.ts:18`) and updates `isAllowedBlueBubblesSender` (`extensions/bluebubbles/src/targets.ts:326`) to opt-in to explicit behavior. Part of a three-commit hardening series. |
| `51c089367` | refactor(security): remove unused empty allowlist mode | 1 prod + 1 test | Removes `emptyAllowFrom` parameter from `isAllowedParsedChatSender` (`src/plugin-sdk/allow-from.ts:18`) after confirming no callers need the legacy 'allow' mode. The function now unconditionally returns `false` (line 29) when `allowFrom` is empty — permanent fail-closed enforcement. |
| `25e89cc86` | fix(security): harden shell env fallback | 3 prod + 4 test + 1 doc | Adds `isTrustedShellPath` at `src/infra/shell-env.ts:44` that validates shell binaries against `TRUSTED_SHELL_PREFIXES` (`/bin/`, `/usr/bin/`, `/usr/local/bin/`, etc.) and `/etc/shells` before using them in shell env resolution. Adds `cachedEtcShells` for performance. Related to GHSA-w9cg-v44m-4qv8 (BASH_ENV/ENV startup-file injection). |
| `ed960ba4e` | refactor(security): centralize path guard helpers | 4 prod + 1 test | Creates NEW `src/infra/path-guards.ts`: `isPathInside` at `:24` (platform-aware path containment, resolves symlinks before comparison), `isSymlinkOpenError` at `:20`, `isNotFoundPathError` at `:16`, `isNodeError` at `:7`. Migrates `archive.ts` and `fs-safe.ts` to shared helpers, reducing risk of inconsistent path checks. |
| `51149fcaf` | refactor(gateway): extract connect and role policy logic | 3 prod + 3 test | Extracts gateway role authorization into NEW `src/gateway/role-policy.ts`: `parseGatewayRole` at `:7` (null for unknown roles), `isRoleAuthorizedForMethod` at `:18`. Extracts connection policy into NEW `src/gateway/server/ws-connection/connect-policy.ts`: `resolveControlUiAuthPolicy` at `:12`, `shouldSkipControlUiPairing` at `:35`. `authorizeGatewayMethod` (`server-methods.ts:35`) now calls `isRoleAuthorizedForMethod`. Updates Feb 21 sync 8 refs: `message-handler.ts:86,109` → `connect-policy.ts:12,35`. |
| `ddcb2d79b` | fix(gateway): block node role when device identity is missing | 1 prod + 2 test | Adds guard in `src/gateway/server/ws-connection/message-handler.ts:494`: `sendHandshakeErrorResponse(ErrorCodes.NOT_PAIRED, "device identity required")` when a node-role connect request lacks device identity. Prevents nodes from connecting without a device binding, closing an identity-bypass path. |
| `f97c45c5b` | fix(security): warn on Discord name-based allowlists in audit | 1 prod + 1 test + docs | Adds `isDiscordNameBasedAllowEntry` at `src/security/audit-channel.ts:22` (detects mutable display names vs. stable numeric IDs) and `addDiscordNameBasedEntries` at `:41`. Audit now warns when operators use username strings instead of user IDs in `allowFrom`, since display names can be changed by users. |
| `4b226b74f` | fix(security): block zip symlink escape in archive extraction | 1 prod + 1 test | Adds `assertNoSymlinkTraversal` at `src/infra/archive.ts:236` which walks each path component looking for symlinks that redirect outside the extraction target directory. `OPEN_WRITE_FLAGS` at `:68` (`O_WRONLY \| O_CREAT \| O_TRUNC`) used after traversal check completes. Fixes malicious archive escape via symlink-to-path indirection. `ArchiveSecurityError` at `:41` is thrown on detection. |
| `e371da38a` | fix(macos): consolidate exec approval evaluation | 6 prod + 0 test + 1 doc | Extracts macOS exec allowlist logic into NEW `apps/macos/Sources/OpenClaw/ExecAllowlistMatcher.swift` and `ExecApprovalEvaluation.swift`. Reduces `ExecApprovals.swift` by 360 lines. Separation of concerns for exec allowlist matching and approval evaluation reduces risk of logic errors in macOS native exec gating. |
| `9fc6c8b71` | fix: hide synthetic untrusted metadata in chat history | 6 prod + 2 test | Adds `UNTRUSTED_CONTEXT_HEADER` sentinel at `src/auto-reply/reply/strip-inbound-meta.ts:25` and updates `stripInboundMetadata` at `:72` to strip `<<<EXTERNAL_UNTRUSTED_CONTENT>>>` blocks from inbound chat history. Prevents injected channel metadata (sender labels, Discord metadata) from being replayed into the AI's context as if written by the user. |
| `afa22acc4` | fix: harden extension relay auth token flow | 2 prod + 2 test | Creates NEW `src/browser/extension-relay-auth.ts`: `deriveRelayAuthToken` at `:26` (HMAC-SHA256 of gateway token + port via `RELAY_TOKEN_CONTEXT = "openclaw-extension-relay-v1"`), `resolveRelayAuthTokenForPort` at `:30`. Replaces ad-hoc relay auth with deterministic HMAC-derived tokens tied to the gateway auth token. Relates to GHSA-pfv7-rr5m-qmv6 (Browser Extension Relay auth). |
| `89aad7b92` | refactor: tighten safe-bin policy model and docs parity | 2 prod + 2 test | Renames `valueFlags`→`allowedValueFlags` and `blockedFlags`→`deniedFlags` in `SafeBinProfile` type (`src/infra/exec-safe-bin-policy.ts:29-30`). Tightens `jq` `allowedValueFlags` to only `--arg`, `--argjson`, `--argstr` (removes `--argfile`, `--rawfile`, `--slurpfile` etc.). Explicit allow/deny naming prevents confusion about whether a flag set is an allowlist or blocklist. |
| `57fbbaebc` | fix: block safeBins sort --compress-program bypass | 3 prod + 3 test | Adds `--compress-program` to `deniedFlags` for `sort` at `src/infra/exec-safe-bin-policy.ts:147`. Also blocks `--files0-from` and `--output` / `-o`. Prevents `sort --compress-program=sh` from launching a shell via the `sort` safeBins allowlist entry, bypassing exec approval. |
| `35a57bc94` | fix: gate doctor oauth-dir repair by channel config | 1 prod + 2 test | Adds `shouldRequireOAuthDir` at `src/commands/doctor-state-integrity.ts:164` which checks channel config for pairing policy before running oauth-dir repair. Prevents the doctor command from attempting oauth-dir repairs on instances that don't require OAuth (e.g., API-key-only deployments), avoiding unintended auth state mutation. |
| `22940b7b9` | refactor(discord): split allowlist resolution flow | 2 prod + 0 test | Refactors `resolveDiscordAllowlistConfig` (`src/discord/monitor/provider.allowlist.ts:236`) into sub-functions: `resolveGuildEntriesByChannelAllowlist` at `:66`, `resolveAllowFromByUserAllowlist` at `:139`, `resolveGuildEntriesByUserAllowlist` at `:186`. Error handling changed: failures return original entries rather than leaving state partially mutated. Reduces risk of allowlist resolution logic errors. |
| `5da03e622` | fix(macos): harden exec allowlist shell-chain checks | 7 prod + 1 test + 1 doc | New `apps/macos/Sources/OpenClaw/ExecApprovals.swift` refactoring hardens exec allowlist shell-chain evaluation on the macOS native runtime. Improves handling of chained shell commands in the macOS exec approval flow. |
| `8178ea472` | feat: thread-bound subagents on Discord (#21805) | 72 prod + 42 test | Introduces thread-bound subagent delivery routing: NEW `src/infra/outbound/bound-delivery-router.ts` with `BoundDeliveryRouter` type at `:21` and `createBoundDeliveryRouter` at `:55`. Subagents are now bound to the originating Discord thread, preventing cross-thread message leakage. Large feature (114 files) primarily in Discord integration and gateway session handling. |

### Key Code Locations

- `src/plugin-sdk/allow-from.ts:18`: `isAllowedParsedChatSender` (fail-closed at line 29 when empty)
- `extensions/bluebubbles/src/targets.ts:326`: `isAllowedBlueBubblesSender`
- `src/infra/shell-env.ts:44`: `isTrustedShellPath`
- `src/infra/path-guards.ts` (NEW): `isNodeError` at `:7`, `isNotFoundPathError` at `:16`, `isSymlinkOpenError` at `:20`, `isPathInside` at `:24`
- `src/infra/archive.ts:41`: `ArchiveSecurityError`; `:68`: `OPEN_WRITE_FLAGS`; `:236`: `assertNoSymlinkTraversal`
- `src/gateway/role-policy.ts` (NEW): `parseGatewayRole` at `:7`, `isRoleAuthorizedForMethod` at `:18`
- `src/gateway/server/ws-connection/connect-policy.ts` (NEW): `resolveControlUiAuthPolicy` at `:12`, `shouldSkipControlUiPairing` at `:35`
- `src/gateway/server-methods.ts:35`: `authorizeGatewayMethod`
- `src/gateway/server/ws-connection/message-handler.ts:494`: "device identity required" error
- `src/security/audit-channel.ts:22`: `isDiscordNameBasedAllowEntry`; `:41`: `addDiscordNameBasedEntries`
- `src/auto-reply/reply/strip-inbound-meta.ts:25`: `UNTRUSTED_CONTEXT_HEADER`; `:72`: `stripInboundMetadata`
- `src/browser/extension-relay-auth.ts` (NEW): `deriveRelayAuthToken` at `:26`, `resolveRelayAuthTokenForPort` at `:30`
- `src/infra/exec-safe-bin-policy.ts:29-30`: `allowedValueFlags`/`deniedFlags` type; `:147`: sort `--compress-program` in `deniedFlags`
- `src/commands/doctor-state-integrity.ts:164`: `shouldRequireOAuthDir`
- `src/discord/monitor/provider.allowlist.ts:66`: `resolveGuildEntriesByChannelAllowlist`; `:139`: `resolveAllowFromByUserAllowlist`; `:186`: `resolveGuildEntriesByUserAllowlist`; `:236`: `resolveDiscordAllowlistConfig`
- `src/infra/outbound/bound-delivery-router.ts` (NEW): `BoundDeliveryRouter` at `:21`, `createBoundDeliveryRouter` at `:55`

## Audit Cross-Reference

| Commit | Audit Claims Affected |
|--------|----------------------|
| `9632b9bcf`, `2ba6de7ea`, `51c089367` | Audit 2 Claim 5 (self-approving agent / RBAC) — allowlist fail-closed enforcement across all channel surfaces |
| `51149fcaf`, `ddcb2d79b` | Audit 2 Claim 5 — gateway role policy extraction and node-role device identity enforcement |
| `f97c45c5b`, `22940b7b9` | Audit 2 Claim 5 — Discord allowlist audit hardening and resolution flow cleanup |
| `4b226b74f`, `ed960ba4e` | Audit 1 Claim 6 (path traversal in agent dirs) — zip symlink escape fix and path guard centralization |
| `89aad7b92`, `57fbbaebc` | Audit 2 Claim 7 (shell injection via incomplete regex) — exec safe-bin policy tightening and sort --compress-program bypass block |
| `25e89cc86` | Audit 2 Claim 8 (env variable injection) — shell env fallback hardening with trusted path validation |
| `afa22acc4` | Audit 1 Claim 7 (webhook signature bypass) — relay auth token now HMAC-derived from gateway token |
| `9fc6c8b71` | Audit 2 Claim 7 — strips injected metadata blocks from chat history (prompt injection via metadata replay) |

**Gap status:** All 3 remaining gaps unchanged. Legitimate Gap 2 (pipe-delimited token format) and Gap 3 (outPath validation in screen_record) remain open. Gap 4 (bootstrap/.md content scanning) remains open.

**Related GHSAs (published 2026-02-21, not yet in advisories.md):** GHSA-w9cg-v44m-4qv8 (BASH_ENV/ENV startup-file injection, addressed by `25e89cc86`), GHSA-pfv7-rr5m-qmv6 (Browser Extension Relay auth inconsistency, addressed by `afa22acc4`), GHSA-5mx2-2mgw-x8rm (BlueBubbles allowlist hardening, addressed by `9632b9bcf`/`51c089367`). 12 new GHSAs total pending addition to official-security-advisories.md.

## Non-Security Commits

| SHA | Message | Files |
|-----|---------|-------|
| `2c14b0cf4` | refactor(config): unify streaming config across channels | 19 prod + 7 test |
| `4c1dd9d06` | fix(security): harden macos rawCommand allowlist resolution | docs-only |
| `6ffca3628` | fix(config): add shared streaming resolver module | 1 prod |
| `747bb581b` | fix(discord): canonicalize resolved allowlists to ids | 4 prod + 1 test |
| `d6353cc54` | fix(ios): suppress expected speech cancellation errors | 1 prod |
| `8a661e30c` | fix(ios): prefetch talk tts segments | 1 prod + 1 test |
| `09d5f508b` | fix(cron): persist delivered flag in job state to surface delivery failures (openclaw#19174) thanks @simonemacario | 6 prod + 1 test + 1 doc |
| `764b1f293` | refactor: simplify relay runtime state | 1 prod + 1 test |
| `5e423b596` | fix: remove false-positive billing error rewrite on normal assistant text (openclaw#17834) thanks @niceysam | 2 prod + 1 test |
| `fa89ae8e9` | fix: stabilize swift protocol generation and flaky tests | 5 prod + 1 test |
| `8588183ab` | test: stabilize docker e2e suites for pairing and model updates | 10 test + 1 prod |
| `166068dfb` | test: add byteplus coding-plan live test | 1 test + 1 prod |
| `310104723` | feat(models): add Gemini 3.1 support | 1 prod + 1 test |
| `581868365` | fix: finish volcengine/byteplus landing polish (#7967) (thanks @funmore123) | 7 prod + 0 test |
| `559736a5a` | feat(volcengine): integrate Volcengine & Byteplus Provider | 18 prod + 3 test |
| `1bd3f01c1` | fix(telegram): guard duplicate bot token accounts | 2 prod + 1 test |
| `b25d3652e` | fix(agents): cap embedded runner retry loop | 3 prod + 1 test |
| `c8466e516` | fix(agents): raise dynamic retry cap budget | 2 prod + 1 test |
| `b520e7ac3` | fix: stabilize docker live model and doctor-switch tests | 1 prod + 2 test |
| `352b5262d` | fix(ci): make docs spellcheck fallback deterministic | 3 prod |
| `bdfb97afa` | chore: prep 2026.2.22 unreleased and publish new npm plugins | chore |
| `efdec3925` | fix: correct MiniMax M2.5 pricing (was ~50x too high) (openclaw#22755) thanks @miloudbelarebia | 2 prod + 1 test |
| `905e355f6` | fix: verify gateway restart health after daemon restart | 4 prod + 1 test |
| `5e34eb98f` | chore: update appcast for 2026.2.21 mac release | chore |
| `74e6c210c` | fix: ignore prerelease suffixes in release-check plugin version checks | 1 prod |
| `e93ba6ce2` | fix: harden update restart service convergence | 1 prod + 1 test |
| `95c14d9b5` | docs: prune low-signal changelog entries | docs |
| `817905f3a` | docs: document thread-bound subagent sessions and remove plan | docs |
| `3ed71d6f7` | fix: update changelog for ios talk tts prefetch (#22833) (thanks @ngutman) | docs |
| `c730d4dd7` | docs: clarify non-default scope for safeBins sort fix | docs |
| `59c78c105` | docs: revert automated heading consistency edits (#22743) | docs |
| `7c1a2ab08` | test: tolerate transient zai and minimax live-model failures | test |
| `d9844c6af` | CI: remove docs spellcheck step (#22738) | CI |

## Feature Changes

- **`8178ea472`** feat: thread-bound subagents on Discord — new Discord threading capability; `bound-delivery-router.ts` routes subagent output to originating thread
- **`310104723`** feat(models): add Gemini 3.1 support — Gemini 3.1 model added to model selector
- **`559736a5a`** feat(volcengine): integrate Volcengine & Byteplus Provider — new provider with `VOLCANO_ENGINE_API_KEY`/`BYTEPLUS_API_KEY` env vars

---

> [Legitimate Gaps Status](../post-merge-hardening.md#legitimate-gaps-status)
