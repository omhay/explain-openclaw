> [Back to Post-Merge Hardening Index](../post-merge-hardening.md) | [Legitimate Gaps](../post-merge-hardening.md#legitimate-gaps-status)

## Post-Merge Hardening (Feb 22 sync 4, 51 commits)

**Merge commit:** 78c7ed829 | **Range:** ace1bf97bb..7bfbbd6309

51 upstream commits (1 security-relevant, 50 non-security). Triage: 32 initially flagged by analyze-commits.sh; v2 COORDINATOR parallel triage (6 batches, 2 waves): 13 demoted, 2 keyword-guarded (retained), 4 promoted — final 23 deep-analyzed; 1 confirmed security-relevant (production compaction fix escalated from Tier B).

### Security-Relevant Commits

| SHA | Message | Files | Security Impact |
|-----|---------|-------|-----------------|
| `b703ea367` | fix: prevent compaction "prompt too long" errors (#22921) | 2 prod + 0 test | Availability: corrects token budget underestimation in compaction chunking that caused failed summarization and potential conversation content loss. |

1. **`b703ea367`** — **fix: prevent compaction "prompt too long" errors (#22921):** Adds `SUMMARIZATION_OVERHEAD_TOKENS = 4096` exported at `src/agents/compaction.ts:77` to reserve budget for the prompt overhead `generateSummary` wraps around each chunk (system prompt, `<conversation>` tags, summarization instructions, `<previous-summary>` block, and `reasoning: "high"` budget). `chunkMessagesByMaxTokens()` (`:79`) now computes `effectiveMax = Math.max(1, Math.floor(maxTokens / SAFETY_MARGIN))` at `:89`, applying the 1.2x `SAFETY_MARGIN` (`:13`) that was previously missing from the chunk-building loop — chunks could be built that fit the estimated token budget but exceeded the actual API limit once properly tokenized. In `src/agents/pi-extensions/compaction-safeguard.ts`, `SUMMARIZATION_OVERHEAD_TOKENS` is subtracted from `maxChunkTokens` at both the dropped-messages summarization path (`:273`) and the main summarization path (`:305`), closing a gap where the `chars/4` token heuristic could silently build oversized chunks that fail at the API level, causing dropped conversation messages or persistent compaction stalls.
   - No audit claim match — availability hardening for the compaction layer against "prompt too long" resource exhaustion.

### Non-Security Commits

| SHA | Message | Files | Reason |
|-----|---------|-------|--------|
| `7bfbbd630` | test(version): dedupe fixture setup and cover invalid URL/version metadata | 0 prod + 1 test | Test fixture refactor |
| `59189750e` | test(browser): dedupe path fixture calls and cover root resolvers | 0 prod + 1 test | Test fixture refactor |
| `0f9ea0229` | test(infra): dedupe install-source fixtures and cover npm pack parsing | 0 prod + 1 test | Test fixture refactor |
| `f9e21d572` | test(infra): dedupe gateway-lock setup and cover guard paths | 0 prod + 1 test | Test fixture refactor |
| `b01335830` | test(pairing): dedupe fixture writers and expand store coverage | 0 prod + 1 test | Test fixture refactor |
| `d35a8b48f` | test(infra): dedupe archive case setup and cover packed-root multi-dir failure | 0 prod + 1 test | Test fixture refactor |
| `822688dc1` | test(infra): dedupe store temp fixtures and cover json5 voicewake sanitization | 0 prod + 1 test | Test fixture refactor |
| `2000dcdcd` | test(memory): dedupe temp-dir lifecycle hooks and cover overlapping path dedupe | 0 prod + 1 test | Test fixture refactor |
| `6051dc10f` | test(scripts): dedupe a2ui temp fixture and cover skip-missing env path | 0 prod + 1 test | Test fixture refactor |
| `d6c2fd545` | test(web): dedupe logout fixture setup and cover non-legacy oauth removal | 0 prod + 1 test | Test fixture refactor |
| `31a0449f6` | test(core): dedupe temp dirs in utils tests and cover lid lookup error fallback | 0 prod + 1 test | Test fixture refactor |
| `c93fc3786` | test(infra): dedupe brew fixtures and cover explicit brew file precedence | 0 prod + 1 test | Test fixture refactor |
| `2042a6921` | test(infra): dedupe dotenv fixture setup and cover fallback-only load | 0 prod + 1 test | Test fixture refactor |
| `d015dc921` | test(cron): dedupe run-log temp fixtures and cover invalid line filtering | 0 prod + 1 test | Test fixture refactor |
| `7036352d9` | test(config): dedupe temp roots and cover legacy state-dir fallback | 0 prod + 1 test | Test fixture refactor |
| `5d61afb36` | test(commands): dedupe signal install extract fixture and cover zip extract | 0 prod + 1 test | Test fixture refactor |
| `3274a1b80` | test(gateway): dedupe control-ui fixture setup and cover query asset 404 | 0 prod + 1 test | Test fixture refactor |
| `8f1b46764` | test(agents): dedupe exec preflight fixtures and cover quoted-path skip | 0 prod + 1 test | Test fixture refactor |
| `8f11868cc` | test(gateway): dedupe boot workspace setup and cover boot failures | 0 prod + 1 test | Test fixture refactor |
| `0e49eec05` | test(commands): dedupe auth-sync fixture and cover invalid profile handling | 0 prod + 1 test | Test fixture refactor |
| `e978297c2` | test(agents): dedupe workspace template temp roots and cover fallback resolution | 0 prod + 1 test | Test fixture refactor |
| `1bbeedfab` | test(infra): dedupe heartbeat ghost reminder temp/mocks setup | 0 prod + 1 test | Test fixture refactor |
| `ac6c344d9` | test(browser): dedupe fixture lifecycle and cover directory-path rejection | 0 prod + 1 test | Test fixture refactor |
| `626d8e9f6` | test(web): dedupe temp dir setup in web auto-reply utils tests | 0 prod + 1 test | Test fixture refactor |
| `bd74d4916` | test(cli): dedupe camera temp fixtures and cover clip url error paths | 0 prod + 1 test | Test fixture refactor |
| `c45ef5f8b` | test(line): dedupe event fixtures and cover room postback routing | 0 prod + 1 test | Test fixture refactor |
| `1794f42ac` | test(config): dedupe io fixture wiring and cover legacy config-path override | 0 prod + 1 test | Test fixture refactor |
| `544a1142b` | test(agents): dedupe skill helper fixtures and cover empty-body rendering | 0 prod + 1 test | Test fixture refactor |
| `c394c5fa9` | test(daemon): dedupe schtasks install fixture and cover empty env omission | 0 prod + 1 test | Test fixture refactor |
| `c481b2224` | test(reply): reuse compaction fixture setup and cover numeric fallback defaults | 0 prod + 1 test | Test fixture refactor |
| `a20c77325` | test(media): dedupe auto-e2e temp/env setup and cover no-binary path | 0 prod + 1 test | Test fixture refactor |
| `b889a5d51` | test(cli): dedupe temp dirs in camera tests and cover non-ok url responses | 0 prod + 1 test | Test fixture refactor |
| `4f835c4c0` | test(media): dedupe temp roots and cover directory attachment rejection | 0 prod + 1 test | Test fixture refactor |
| `0a207b986` | refactor(test): share temp workspace helper in compact skill path tests | 0 prod + 1 test | Test fixture refactor |
| `324922f80` | refactor(test): dedupe temp dir lifecycle in agents skills directory e2e | 0 prod + 1 test | Test fixture refactor |
| `85c768d3d` | refactor(test): dedupe temp workspace setup in skills load entries e2e | 0 prod + 1 test | Test fixture refactor |
| `040176214` | refactor(test): dedupe temp root setup in identity avatar e2e | 0 prod + 1 test | Test fixture refactor |
| `70fdab6e9` | test(agents): add coverage for shared skill writer helper | 0 prod + 1 test | Test fixture refactor |
| `a418c6db0` | test(agents): dedupe agent-path fixtures and cover env override precedence | 0 prod + 1 test | Test fixture refactor |
| `6fd31fc0b` | test(browser): dedupe invalid-path assertions and cover blank path rejection | 0 prod + 1 test | Test fixture refactor |
| `bdfb97994` | test(cli): dedupe camera fetch stubs and cover empty-body download rejection | 0 prod + 1 test | Test fixture refactor |
| `9ebfc99c1` | refactor(test): dedupe temp media fixture setup in apply e2e | 0 prod + 1 test | Test fixture refactor |
| `b3c7fd6c6` | refactor(test): dedupe temp dirs and skill writer in snapshot e2e | 0 prod + 1 test | Test fixture refactor |
| `9ead79937` | refactor(test): dedupe temp session path setup in file repair e2e | 0 prod + 1 test | Test fixture refactor |
| `518dbbf4c` | test: avoid template-literal temp path in runner fixture | 0 prod + 1 test | Test fixture refactor |
| `0ecb07e6d` | test(cli): dedupe acp secret file setup and cover password flag collisions | 0 prod + 1 test | Test fixture refactor |
| `302fa03f4` | fix(test): skip test-utils files in temp path guard | 0 prod + 1 test | Test-scope fix |
| `48ddb1cc8` | fix(ci): stabilize install smoke in docker | 0 prod + 3 CI | CI stabilization |
| `549549f6a` | fix(ci): sync plugin versions and harden install smoke | 0 prod + 40 CI/config | Version bump + CI |
| `ac633366c` | docs: add Onur Solmaz to contributors (#22890) | 0 prod + 1 doc | Contributors doc |

### Audit Cross-Reference

The one security-relevant commit does not address any of the 16 original audit claims or close the remaining legitimate gaps. It represents new availability hardening for the compaction layer.

- **b703ea367**: No claim match — prevents compaction-layer resource exhaustion by correcting token budget accounting in `src/agents/compaction.ts` and `src/agents/pi-extensions/compaction-safeguard.ts`.

**Gaps remain:** 3 open (pipe-delimited token format, `outPath` validation in `screen_record`, `.md` content scanning).

### Phase 6c: New Official Security Advisories (Feb 22 sync 4)

12 new GHSAs were published on 2026-02-21 and added to `official-security-advisories.md`:

- **GHSA-hff7-ccv5-52f8** (MEDIUM): Gateway tokenless Tailscale auth applied to HTTP routes
- **GHSA-45cg-2683-gfmq** (MEDIUM): Browser navigation guard allowed non-network URL schemes (file://, data:// access)
- **GHSA-5v6x-rfc3-7qfr** (MEDIUM): Windows system.run approval mismatch on cmd.exe /c trailing arguments
- **GHSA-5mx2-2mgw-x8rm** (MEDIUM): BlueBubbles beta plugin webhook auth hardening (remove passwordless fallback)
- **GHSA-62f6-mrcj-v8h5** (LOW): Runtime /debug override path accepted prototype-reserved keys
- **GHSA-3cvx-236h-m9fj** (MEDIUM): Opt-in insecure Control UI auth over plaintext HTTP
- **GHSA-w9cg-v44m-4qv8** (MEDIUM): BASH_ENV / ENV startup-file injection into spawned shell commands
- **GHSA-8fmp-37rc-p5g7** (MEDIUM): Config env vars allowed startup env injection into service runtime
- **GHSA-2rgf-hm63-5qph** (LOW): Improper X-Forwarded-For parsing behind trusted proxies (client IP spoofing)
- **GHSA-43x4-g22p-3hrq** (MEDIUM): Chrome --no-sandbox disabled OS-level browser sandbox in sandbox container
- **GHSA-25gx-x37c-7pph** (MEDIUM): Sandbox browser noVNC observer lacked VNC authentication
- **GHSA-cjv3-m589-v3rx** (MEDIUM): Canvas route hardening for mixed-trust deployments

None of these new GHSAs relate to commits in this sync range.

> See [Legitimate Gaps Status](../post-merge-hardening.md#legitimate-gaps-status) for full gap details.
